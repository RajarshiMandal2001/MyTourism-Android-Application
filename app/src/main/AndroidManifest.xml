<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.mytourism">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyTourism">
        <activity android:name=".PlaceCategoryActivity"></activity>
        <activity android:name=".AdminAllHotelsActivity" />
        <activity android:name=".AdminAllPlacesActivity" />
        <activity android:name=".ViewAllActivities" />
        <activity android:name=".AuthenticateAdminActivity" />
        <activity android:name=".AuthenticateExplorer" />
        <activity android:name=".GeneratePasswordActivity" />
        <activity android:name=".HotelSubscriptionActivity" />
        <activity android:name=".NoValueActivity" />
        <activity android:name=".AddReviewActivity" />
        <activity android:name=".ReviewActivity" />
        <activity android:name=".ViewReviewsActivity" />
        <activity android:name=".AddedHotels" />
        <activity android:name=".ViewHotelActivity" />
        <activity android:name=".AddNewHotelActivity" />
        <activity android:name=".AddedPlaces" />
        <activity android:name=".ViewPlaceActivity" />
        <activity android:name=".AddNewPlaceActivity" />
        <activity android:name=".UserTypeActivity" /> <!-- <activity android:name=".MainActivity"></activity> -->
        <activity android:name=".splashscreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />

        <service
            android:name=".FirebaseMessagingService"
            android:permission="TODO"
            tools:ignore="ExportedService">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

    </application>

</manifest>